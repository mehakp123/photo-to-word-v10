{
  "name": "photo-to-word-android-v15",
  "version": "1.0.15",
  "private": true,
  "description": "Photo to Word Android app with faster photo processing, highlighted Save/Bluetooth options, direct save, resized pictures, editable Word, and Bluetooth sharing.",
  "scripts": {
    "sync": "cap sync android",
    "build:android": "cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "latest",
    "@capacitor/cli": "latest",
    "@capacitor/core": "latest"
  },
  "devDependencies": {}
}